CREATE TABLE employee
(
   id INTEGER GENERATED BY DEFAULT AS IDENTITY,
   Code VARCHAR (255),
   Name VARCHAR (255),
   Date_Of_Join VARCHAR (255),
   Grade VARCHAR (255),
   Salary VARCHAR (255),
   Status VARCHAR (255),
   PRIMARY KEY (id)
);